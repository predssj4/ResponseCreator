using System;
using System.Collections.Generic;
using System.Text;
using ResponseCreator.Abstract;
using ResponseCreator.Tests.Fakes;
using Xunit;

namespace ResponseCreator.Tests.ValidationTests
{
    public class BoolValidationTests
    {
        //[Fact]
        //string key = "input_bool_key";

        //IResponseCreator responseCreator = new ResponseCreator();
        //InputValidator<InputWithBoolDTO> objectUnderTest = new InputValidator<int>(inputValue, responseCreator);

        //// act
        //objectUnderTest.ForInt(x => x, key)
        //.InRange(min, max);

        //// assert
        //responseCreator.GetValidationResultsForKey(key).Any().ShouldBe(hasError, displayMessage);
    }
}
